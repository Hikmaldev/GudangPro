{
  "name": "inventori-gudang",
  "private": true,
  "scripts": {
    "dev": "npm start --prefix frontend",
    "build": "npm run build --prefix frontend",
    "start": "npm start --prefix frontend",
    "backend": "set PATH=C:\\Program Files\\dotnet;%PATH% && dotnet run --project backend/src/GudangPro.Api",
    "backend:test": "set PATH=C:\\Program Files\\dotnet;%PATH% && dotnet test backend",
    "backend:build": "set PATH=C:\\Program Files\\dotnet;%PATH% && dotnet build backend",
    "docker:up": "docker compose up -d --build",
    "docker:down": "docker compose down",
    "docker:logs": "docker compose logs -f"
  }
}
